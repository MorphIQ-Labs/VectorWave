.level=INFO

# Console handler outputs to stderr by default
handlers= java.util.logging.ConsoleHandler
java.util.logging.ConsoleHandler.level=INFO
java.util.logging.ConsoleHandler.formatter=java.util.logging.SimpleFormatter

# Reduce VectorWave logs in CI or tests
com.morphiqlabs.wavelet.level=WARNING

# Example: elevate performance calibrator to INFO locally
# com.morphiqlabs.wavelet.performance.level=INFO

